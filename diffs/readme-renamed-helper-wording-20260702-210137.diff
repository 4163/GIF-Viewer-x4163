diff --git "C:\\Users\\x4163\\Desktop\\gifviewer\\backups\\PROJECT_NOTES.md.20260702-210137.bak" "C:\\Users\\x4163\\Desktop\\gifviewer\\README.md"
index 966263b..0cc0c4d 100644
--- "C:\\Users\\x4163\\Desktop\\gifviewer\\backups\\PROJECT_NOTES.md.20260702-210137.bak"
+++ "C:\\Users\\x4163\\Desktop\\gifviewer\\README.md"
@@ -1,8 +1,8 @@
 # GIF Viewer Helper Predecode Notes
 
-## Scope
+## GIF Viewer x4163
 
-This folder contains an experimental source build of the SourceForge GIF Viewer project with a Quivi-style helper predecode path.
+This folder contains a modified source build of the SourceForge GIF Viewer project with a Quivi-style helper predecode path.
 
 This prepared project package was generated by OpenAI's Codex using GPT 5.5 at a Medium Reasoning level.
 
@@ -13,23 +13,28 @@ https://sourceforge.net/projects/gifviewer/
 https://git.code.sf.net/p/gifviewer/git
 ```
 
+## Quivi Reference
+
+- Original project: [Quivi](http://quivi.sourceforge.net/)
+- Later continuation/fork used for comparison: [qazmlpok/quivi](https://github.com/qazmlpok/quivi)
+
 ## Folder Layout
 
 ```text
 gifviewer/
-  GIF Viewer.exe        runnable build copied to the root
-  GifComponents/        runtime support folder for the runnable build
+  GIF Viewer.exe        root runnable build
+  GifComponents/        runtime support folder for the root runnable build
   webp_extract_pillow_runtime/
   webp_extract_pillow.exe
-  source/               cloned source, with local experimental changes
+  source/               source tree with local changes
   diffs/                change notes and hashes
-  backups/              backups of edited source files
+  backups/              backups of edited source files and archived build-output files
   tools/                smoke-test helper script
 ```
 
-## Current Experiment
+## Helper Predecode Implementation
 
-The active experiment is in:
+The main code path is in:
 
 ```text
 source/GIFFile.cs
@@ -48,11 +53,11 @@ Helper lookup order:
 1. `webp_extract_pillow_runtime/webp_extract_pillow/webp_extract_pillow.exe`
 2. `webp_extract_pillow.exe`
 
-## Reverted Experiment
+## Historical Change Notes
 
-An earlier experiment forced nearest-neighbor scaling while playback was running.
+An earlier change forced nearest-neighbor scaling while playback was running.
 
-That was reverted because it did not improve performance enough and made the quality selector appear ineffective during playback.
+That change was reverted because it did not improve performance enough and made the quality selector appear ineffective during playback.
 
 The note is kept for history:
 
@@ -60,7 +65,7 @@ The note is kept for history:
 diffs/gifviewer-playback-fast-scaling.diff
 ```
 
-The active helper experiment is documented here:
+The active helper predecode change is documented here:
 
 ```text
 diffs/gifviewer-helper-predecode.diff
@@ -79,7 +84,7 @@ The project targets .NET Framework 4.5 and was built locally with VS 2022 MSBuil
   /m
 ```
 
-The runnable output from the build folder was copied to the repository root so the modified viewer can be launched without digging through the source tree. The original build folder remains in place so the build process is still understandable.
+The runnable output was moved to the repository root so the modified viewer can be launched directly. The source tree remains in place so the build process is still understandable.
 
 ## Runtime Files
 
@@ -111,7 +116,7 @@ RUNNING=True
 SAW_HELPER=True
 ```
 
-## Tradeoffs
+## Performance Notes
 
 The helper path shifts work from playback time to load time.
 
@@ -125,11 +130,9 @@ Costs:
 
 - Higher initial load time.
 - Higher RAM use, because full frames are materialized.
-- Still experimental with large real-world GIFs.
+- Large real-world GIFs still need broader validation.
 
-## Next Steps
-
-The next technical step is to fix and optimize the resampling behavior used by the scaling methods. The helper predecode path improves playback, but expensive scaling can still dominate frame presentation for large images. Future work should keep the quality selector functional while reducing the cost of high-quality resizing.
+Future scaling work should fix and optimize the resampling behavior used by the scaling methods. The helper predecode path improves playback, but expensive scaling can still dominate frame presentation for large images. Future work should keep the quality selector functional while reducing the cost of high-quality resizing.
 
 ## Public Repository Hygiene
 
@@ -138,8 +141,7 @@ Before publishing the repository:
 - Keep `source/`, `diffs/`, `backups/`, and `tools/` if sharing development history.
 - Keep the root runnable build if sharing a ready-to-run copy.
 - Do not include private test GIFs.
-- Keep helper runtime files only if distributing a runnable experimental build.
+- Keep helper runtime files only if distributing a runnable modified build.
 - Review licenses for GIF Viewer and the helper runtime dependencies.
 
 No private test paths or personal sample names should remain in the notes/scripts.
-
